# 1. 默认设置：自动检测文本文件，并在提交时标准化 (推荐)
* text=auto

# 2. 强制 War3 的 JASS 脚本在任何系统下都保持 Windows 换行符 (CRLF)
#    这对 CRC 校验至关重要！同时保留了 diff 功能。
*.j text eol=crlf
*.J text eol=crlf
*.lua text eol=crlf

# 3. 强制二进制文件 (严禁 Git 修改任何字节)
*.mpq binary
*.w3x binary
*.w3m binary
*.w3g binary
*.dll binary
*.exe binary